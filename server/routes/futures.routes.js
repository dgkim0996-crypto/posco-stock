import{Router}from"express";import{closeFuture,getFutures,openFuture}from"../controllers/futures.controller.js";import{requireAccount}from"../middleware/auth.js";const router=Router();router.use("/accounts",requireAccount);router.get("/accounts/me/futures",getFutures);router.post("/accounts/me/futures",openFuture);router.post("/accounts/me/futures/:positionId/close",closeFuture);export default router;
