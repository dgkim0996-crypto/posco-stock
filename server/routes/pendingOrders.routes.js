import{Router}from"express";import{amendPendingOrder,cancelPendingOrder,createPendingOrder,getPendingOrders}from"../controllers/pendingOrders.controller.js";import{requireAccount}from"../middleware/auth.js";const router=Router();router.use("/accounts",requireAccount);router.get("/accounts/me/pending-orders",getPendingOrders);router.post("/accounts/me/pending-orders",createPendingOrder);router.patch("/accounts/me/pending-orders/:pendingId",amendPendingOrder);router.delete("/accounts/me/pending-orders/:pendingId",cancelPendingOrder);export default router;
